import React, { useRef } from "react";
import { useFrame } from "@react-three/fiber";
import * as THREE from "three";

const TheSpirit = () => {
  const meshRef = useRef();
 

  return (
    <div>

    </div>
  );
};

export default TheSpirit;
